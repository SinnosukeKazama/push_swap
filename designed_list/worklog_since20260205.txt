2/23 ＜最適化＞
ARG=$(shuf --input-range=-2147483648-2147483647 -n 500 | tr '\n' ' ')

or

ARG=$(python3 - <<'PY'
import random
print(' '.join(str(random.randint(-2**31, 2**31-1)) for _ in range(500)))
PY
)

base-4:
0,1 の1bit二種類から、
00, 01, 10, 11 の2bits4種類としてみる。

3,5要素のとき、専用のソートが必要。